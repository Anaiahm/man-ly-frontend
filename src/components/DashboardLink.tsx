import './DashboardLink.css';

function DashboardLink() {
    // Presentational only: leave click handling to parent to avoid nested buttons
    return <span className="dashboard-link">Dashboard</span>;
}

export default DashboardLink;